import { Outlet } from "react-router-dom"
import './styles/Layout.css'

const Layout = () => {
    return (
        <>
            <nav>
                
            </nav>
            <Outlet />
        </>
    )
}

export default Layout